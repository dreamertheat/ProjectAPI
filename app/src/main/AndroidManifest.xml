<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.jamesreubengruta.projectapi" >
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_web"
        android:name=".framework.di.MasterApplication"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_web"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".presentation.activities.unused.MainActivity" >

        </activity>
        <activity android:name=".presentation.activities.unused.SecamActivity" >

        </activity>

        <activity android:name=".presentation.activities.login_profile.LoginProfile" >

        </activity>
        <activity android:name=".presentation.activities.login_profile.WebMaster" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>